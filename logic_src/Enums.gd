# Enums.gd
# Global reference for constant enums

extends Reference
class_name Enums

enum Actors {PLAYER, RING, CURRENT_CHARACTER, NONE}

enum Elements {RED, GREEN, BLUE, YELLOW, NONE}

enum Phases {GAME_START, TURN_START, PLAY_PHASE, TURN_END, GAME_END}
